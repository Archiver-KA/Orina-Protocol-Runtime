s = s.replace(`  const categoryOptions = useMemo(() =    const values = new Set(COLLECTION_CATEGORY_OPTIONS);\n    if (collection?.category) values.add(collection.category);\n    return Array.from(values).map((value) = value, label: value }));\n  }, [collection?.category]);`, `  const categoryOptions = useMemo(function () {\n    const values = new Set(serverCategoryValues);\n    const currentCategory = String(collection?.category 
